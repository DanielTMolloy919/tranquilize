# Tranquilize Environment Configuration

# Remote Config Development Mode
# Set to 'true' to use local dev server, 'false' to use production GitHub URL
# When true, config is fetched from http://localhost:3001/remote-config.json
# When false, config is fetched from GitHub
VITE_USE_DEV_CONFIG=false

# Note: Copy this file to .env for local development
# The .env file is gitignored and won't be committed
